import 'package:flutter/material.dart';

class lop extends StatelessWidget {
  const lop({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        width: 300,
        height: 4000,
        color: Colors.red,
      ),
    );
  }
}
